.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install sortable

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv sortable
    $ pip install sortable
